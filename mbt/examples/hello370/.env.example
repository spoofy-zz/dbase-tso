# hello370 — local configuration
# Copy to .env and fill in your values.
# .env is gitignored and never committed.

# MVS connection (mvsMF REST API)
MBT_MVS_HOST=localhost
MBT_MVS_PORT=1080
MBT_MVS_USER=IBMUSER
MBT_MVS_PASS=

# High-level qualifier for build datasets
MBT_MVS_HLQ=IBMUSER
